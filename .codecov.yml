coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        target: 90%
        threshold: 0%
        informational: false

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true
